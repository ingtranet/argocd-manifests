apiVersion: v1
kind: Service
metadata:
  name: tei
  labels:
    app: tei
spec:
  selector:
    app: tei
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
  type: ClusterIP
