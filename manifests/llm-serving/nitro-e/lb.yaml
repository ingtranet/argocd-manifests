apiVersion: v1
kind: Service
metadata:
  name: nitro-e-lb
  namespace: llm-serving
spec:
  type: ExternalName
  externalName: traefik.traefik.svc.kl.ingtra.net
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP

---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: nitro-e-lb
  namespace: llm-serving
spec:
  entryPoints:
    - web
  routes:
    - kind: Rule
      match: Host(`nitro-e-lb`)
      priority: 100
      services:
        - kind: Service
          name: nitro-e
          namespace: llm-serving
          port: 8000
