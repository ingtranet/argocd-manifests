apiVersion: v1
kind: Service
metadata:
  name: embeddinggemma-llamacpp
  labels:
    app: embeddinggemma-llamacpp
spec:
  selector:
    app: embeddinggemma-llamacpp
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
  type: ClusterIP
