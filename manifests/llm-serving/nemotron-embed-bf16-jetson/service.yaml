apiVersion: v1
kind: Service
metadata:
  name: nemotron-embed-bf16-jetson
  labels:
    app: nemotron-embed-bf16-jetson
spec:
  selector:
    app: nemotron-embed-bf16-jetson
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
  type: ClusterIP
